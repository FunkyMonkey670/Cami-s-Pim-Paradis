# Reference Catalog

This directory is the lookup layer for exact contracts shared across source files. It prevents contributors from inventing duplicate names, incompatible types, conflicting writers, or undocumented meanings.

Module pages explain why and how a feature behaves. Reference pages define the precise names and shapes that code relies on.
